name: Publish Docker image

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/**"
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    uses: GitFabien/platform-demo-lab1/.github/workflows/ci-thedasken.yml@main
